apiVersion: v1
kind: Service
metadata:
  name: pulse-api
  labels:
    app: pulse-api
spec:
  type: ClusterIP
  selector:
    app: pulse-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
